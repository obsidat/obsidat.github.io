{
  "name": "svelte-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5050",
    "build": "vite build",
    "preview": "vite preview --port 5050",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "svelte-check": "^3.6.0"
  },
  "dependencies": {
    "svelte-spa-router": "^4.0.0"
  }
}
